name: Cloud infra deployment

on:
  workflow_dispatch:
  push:
    paths:
      - 'infra/**'
    branches: 
      - 'vcptcore-*'

jobs:
  gitops:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
   
      - name: Install vc-build
        run: |
          dotnet tool install --global VirtoCommerce.GlobalTool

      - name: Update app
        run: |
          vc-build CloudEnvUpdate -CloudToken ${{ secrets.VCPTCORE_PLATFORM_TOKEN }} -ArgoConfigFile $ENV_FILE -CloudUrl https://portal-uat.govirto.com
        env:
          ENV_FILE: './infra/environments.yml'
          ARGO_SERVER: https://argo.virtocommerce.cloud
          TOKEN: ${{ secrets.VCPTCORE_TOKEN }}
